# @agnishc/edb-protocol

Shared, versioned event protocol definitions for EDB Pi extensions. The initial `todo/v1` protocol validates capability discovery and correlated task requests/responses between `@agnishc/edb-subagents` and `@agnishc/edb-todo`.

This package contains protocol types and runtime validators only. It does not read task files or start a transport process.
